const submitGuestForm=(form,endpoint,feedback)=>{form.addEventListener('submit',async event=>{event.preventDefault();const target=form.querySelector(feedback);target.textContent='Mengirim...';try{const response=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const data=await response.json();target.textContent=data.message||'Selesai.';if(data.ok)form.reset()}catch(error){target.textContent='Koneksi sedang bermasalah.'}})};
const rsvpForm=document.querySelector('[data-rsvp-form]');if(rsvpForm)submitGuestForm(rsvpForm,'/api/rsvp.php','[data-rsvp-feedback]');const wishesForm=document.querySelector('[data-wishes-form]');if(wishesForm)submitGuestForm(wishesForm,'/api/wishes.php','[data-wishes-feedback]');
