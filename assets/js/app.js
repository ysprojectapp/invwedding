document.addEventListener('click', event => { const trigger = event.target.closest('[data-menu]'); if (trigger) document.querySelector('[data-nav]')?.classList.toggle('is-open'); const copy = event.target.closest('[data-copy]'); if (copy) navigator.clipboard?.writeText(copy.dataset.copy).then(() => { copy.textContent = 'Tersalin'; setTimeout(() => copy.textContent = 'Salin', 1500); }); });