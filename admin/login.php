<?php
require_once __DIR__ . '/../includes/functions.php';
if (session_status() !== PHP_SESSION_ACTIVE) session_start(); $error = '';
if ($_SERVER['REQUEST_METHOD'] === 'POST') { verify_csrf(); $q = db()->prepare('SELECT * FROM admins WHERE email=? AND status="active" LIMIT 1'); $q->execute([trim($_POST['email'] ?? '')]); $admin=$q->fetch(); if ($admin && password_verify($_POST['password'] ?? '',$admin['password_hash'])) { $_SESSION['admin_id']=$admin['id']; $_SESSION['admin_name']=$admin['name']; header('Location: /admin'); exit; } $error='Email atau password salah.'; }
?><!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Admin Login</title><link rel="stylesheet" href="/assets/css/app.css"></head><body><main class="section narrow"><span class="eyebrow">WAKTUTEMU.ID / ADMIN</span><h1>Selamat datang kembali.</h1><?php if($error): ?><p class="notice"><?= e($error) ?></p><?php endif; ?><form method="post" class="form-card"><?= csrf_field() ?><label>Email<input required type="email" name="email"></label><label>Password<input required type="password" name="password"></label><button class="button">Masuk</button></form></main></body></html>
